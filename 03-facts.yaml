- name: Demo on facts
  hosts: all
  gather_facts: False
  tasks:
    - name: for the distro
      debug:
        msg: "{{ansible_distribution}}"
    - name: for the mac address
      debug:
        msg: "{{ansible_default_ipv4.macaddress}}"